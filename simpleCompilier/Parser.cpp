#include "Parser.h"

ParserClass::ParserClass(ScannerClass * sc, SymbolTableClass * st)
{};
void ParserClass::Start()
{
	/*
	Match(VOID);
	Match(MAIN);
	Match(LPAREN);
	Match(RPAREN);
	*/
};