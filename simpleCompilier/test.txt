void main()
{ 

	/* 
	*
	Test a block //comment*
	**/
	// tEST SINGLE // line comment */
	// test +-*/

	int sum; 
	sum = 35 + 400; 
	//cout << sum; 


	// + * - /

	int subtract;
	subtract = 40-2;
	int times;
	times = 5*5;
	int divide;
	divide = 25/5;

	// test <  > == <= >= !=

	bool less;
	less = 5 < 40;
	bool greater;
	greater = 40 > 5;
	bool equal;
	equal = 5==5;
	bool lessequal;
	lessequal = 5<=6;
	bool greaterequal;
	greaterequal = 6>=5;
	bool notequal;
	notequal = (5!=6);

	// if while

	int x;
	x = 5;
	if(sum <= 400)
	{
		cout << sum; 
	}
	if(x)
		cout << x;
	
	while( x>0 )
	{
		cout << x;
		x = x-1;
	}
	if(x)
		cout << x;
	// && ||
	if( (x<=5) && (400<sum) )
	{
		cout << sum; 
	}
	if(  (400>sum) || (x!=5) )
	{
		cout << x; 
	}

};//